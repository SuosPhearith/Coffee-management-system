<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /coffee-management-system/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ /coffee-management-system/index.html [L]
</IfModule>

